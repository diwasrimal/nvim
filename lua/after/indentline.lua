require("ibl").setup({
    enabled = true,
    -- buftype_exclude = { "terminal", "nofile" },
    indent = {
        char = "▏",
        highlight = 'Comment',
    },
    scope = {
        enabled = false,
    }
})

